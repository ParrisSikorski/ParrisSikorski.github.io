<?php

$db = mysqli_connect('localhost', 'root', '', 'final_project') or die("Could not connect");

// if (!$db)

// 	die ('no db');

// if(!mysqli_select_db("final_project"))

// 	die("No database selected");

?>